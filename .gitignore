# Gradle
.gradle/
build/

# Local configuration (never commit)
local.properties
keystore.properties

# Native core binaries (produced by scripts/build-core.sh, committed by integration)
*.so

# IDE
.idea/
.kotlin/
captures/
.DS_Store

# Build outputs
*.apk

# Go toolchain cache (fetch-go.sh)
.go-cache/

# Local knowledge graph (rebuild with: graphify update .)
graphify-out/
